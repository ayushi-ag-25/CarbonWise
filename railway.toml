[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && gunicorn carbonwise.wsgi --bind 0.0.0.0:$PORT --workers 2"
healthcheckPath = "/api/health/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
